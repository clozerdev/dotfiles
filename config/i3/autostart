# Autostart of some programs when i3 starts
# exec_always	-> execute when reloading i3 or any configuration
# exec			-> execute only once when starting i3

exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id /usr/bin/xwallpaper --zoom $HOME/.config/wallpapers/dragon.jpg
